---
name: sjtu-hw-to-latex
description: 把作业 PDF 识别转写成 LaTeX（输出 .tex / .txt），对照题目核对并订正解答，再渲染成每题一张 PNG。当用户说「把作业转成 LaTeX」「PDF 转 latex」「识别成 latex / 转写文字」「转成 latex 再切题」「对照题目改我的解答」时使用。若用户只想按题裁原始扫描图、不要识别和重排，请改用 sjtu-hw-split。
---

# 作业 PDF → LaTeX → 分题图片

把一份作业 PDF 走完这条流水线：

```
PDF ──拆页──▶ 每页 PNG ──你读图转写──▶ LaTeX txt ──对照题目订正──▶ 渲染 ──▶ 每题一张 PNG
```

**看图转写这一步由你（Claude）亲自做**，不需要 API key、不需要额外脚本。
下面两个脚本只负责两端：PDF 拆页、LaTeX 出图。

技能目录：`~/.claude/skills/sjtu-hw-to-latex`（下文记作 `$SKILL`）。
第一次跑 `hw_render.py` 时它会自己 `npm install`（约 75MB，一两分钟），以后不再需要。

---

## 第 0 步：确认输入

**必问清楚两件事**，别自己猜：

1. **作业 PDF 在哪？** —— 用户可能直接给路径，也可能说「就在某个文件夹里」。
2. **题目（题面）有没有？** —— 决定要不要做第 3 步的订正。三种情况：
   - 同一个 PDF 里既有题面又有解答 → 直接转写全部，第 3 步自动做
   - 题面在另一个 PDF 或一堆图片里 → 让用户给路径
   - 只有解答、没有题面 → **跳过第 3 步**，只转写，并在报告里说明「未对照题目核对」

工作目录定为 `<PDF 所在目录>/<作业名>_转换/`，里面：

```
页面/           PDF 每页渲染图
latex/作业.txt  只有正文（喂给渲染脚本）
latex/作业.tex  完整可编译版（附赠）
图片/第N题.png  最终交付物
报告.md         转写说明 + 订正记录 + 待核对点
```

---

## 第 1 步：PDF 拆页

```bash
python "$SKILL/scripts/hw_pdf.py" "<作业.pdf>" "<工作目录>/页面"
```

输出每行一条：`PAGE 页码 宽x高 路径`，空白页标 `SKIP`（自动跳过，省时间）。
长边默认 1568px —— 这是视觉输入的有效上限，调大没意义。

---

## 第 2 步：读图转写成 LaTeX

**用 Read 工具逐页读 `<工作目录>/页面/page*.png`**，把所有页合并转写成一份 LaTeX。

一次读一页，边读边写，最后拼成完整文件。页数多时（>8 页）分段转写再拼接，
但**分页重复要去重**（见下）。

### 格式约定（渲染脚本依赖这些，必须遵守）

| 要求 | 写法 |
|---|---|
| 每道大题开头 | **单独占一行** `\noindent\textbf{3.}` ← 切题就靠它 |
| 小题号 | 写在正文里，`(1)` `(2)` 这种 |
| 行内公式 | `$...$` |
| 独立公式 | `\[ ... \]` |
| 矩阵 | `\begin{bmatrix} ... \end{bmatrix}` |
| 方程组 | `\begin{cases} ... \end{cases}` |
| 行变换箭头 | `\xrightarrow{r_2-2r_1}` |
| 枚举 | `\begin{enumerate}` / `\begin{itemize}` 可用 |

### 禁止事项

- **不要写标题区**（`\begin{center}`、`{\Large ...}`、`\maketitle`）—— 每题一张图不需要标题
- **不要自定义命令**（`\newcommand`）—— 渲染端不认识
- **公式里不要放中文**。`\text{方程无解}` 能显示，但 MathJax 会退回系统字体，
  字形和正文对不齐。中文一律写在公式外面。
  （`\begin{cases} p=0，则… \end{cases}` 这种也尽量避免，改成公式外面加一句中文）
- 别用 `align` / `gather` 环境（虽然渲染端做了兜底，但排版效果不如拆成多个 `\[...\]`）

### 转写原则

- **忠实**。这一阶段只做「抄写」，不改对错、不补步骤、不润色。订正是第 3 步的事。
- **手写笔迹按最合理的数学读法还原**：潦草的分数写成 `\frac{}{}`，上下标、希腊字母、
  向量、行列式都还原规范。但只还原写法，不改内容。
- **分页重复只保留一次**：扫描/拍照件里，同一段文字、同一个矩阵常在上页末尾和
  下页开头各出现一次。合并成一份时要去重。这是最容易出错的地方，逐页比对时留意。
- **页眉页脚不要**：页码、日期、「第X页」、「某某课程 文档N」这类全丢掉。
- **承接上一页的内容要归位**：第 N 页开头那个矩阵可能是第 N−1 页某道题的延续，
  归到它所属的题目下面，不要留在文件开头。

写完后：

- 正文存 `<工作目录>/latex/作业.txt`
- 再包一个可编译版本存 `latex/作业.tex`（`ctex` + `amsmath` + `amssymb` 导言区，
  `xelatex` 能编译），方便用户以后自己转 PDF

---

## 第 3 步：对照题目核对并订正

**只有在拿得到题面时才做。** 拿不到就跳过，报告里写明。

做法：

1. 读题面（PDF 就拆页读图，图片就直接读）
2. 逐题比对，判断解答的**对错**——不是「像不像」，是真的算一遍/验一遍
3. 错的、漏的、不完整的，**按用户原有解答的思路改**，不要另起炉灶换解法。
   用户要的是「我的解法修对了」，不是「标准答案」
4. 每处改动在 `作业.txt` 里留一行 LaTeX 注释说明：

```latex
% 【改正】原稿这里把无解与无穷多解的条件弄反了；最后一行为 (0 0 0 | q-2)，
%        q≠2 时 0=q-2 矛盾，应为无解。
```

5. 主动记下**自己不确定的**（字迹看不清、符号有歧义、疑似原稿笔误）——
   这些放进报告，别硬猜

常见的漏做类型，值得重点查：
- 题目要求「化为**简化**行阶梯形矩阵」，解答只做到行阶梯形
- 讨论题只答了一半情况（如 `a=3` 漏了 `a=-3`）
- 齐次/非齐次方程组算到最后没写结论或通解
- 「有解时求所有解」只给了唯一解，漏了无穷多解的通解

---

## 第 4 步：渲染成每题一张图

```bash
python "$SKILL/scripts/hw_render.py" "<工作目录>/latex/作业.txt" "<工作目录>/图片"
```

输出每行一条：`PROB 序号 题号 图片路径`，最后 `DONE 题数`。
`WARN` 开头的行是提示（公式里混了中文、某行公式超宽），读一下但通常不影响交付。

脚本做的事：按 `\noindent\textbf{}` 切题 → 每题一个 HTML → MathJax 排版 →
本机 Chrome/Edge 无头截图（2 倍分辨率，2240px 宽）。

渲染完**自己看一两张图确认**（尤其含矩阵、`cases`、多层分数的题），
别不看就交付。

---

## 第 5 步：写报告

在 `<工作目录>/报告.md` 里写清楚：

```markdown
# 作业转换报告

- 源文件：xxx.pdf
- 页数 / 题数
- 产物：latex/作业.txt、latex/作业.tex、图片/第N题.png

## 订正记录
- 第 7 题 (2)：①②③ 条件弄反了，已按 p、q 重新分类
- 第 3 题：题目要求简化行阶梯形，原稿只到行阶梯形，已续做

## 待你核对
- 第 9 题那个符号像「⊂」也像「⊆」，按上下文取了 ⊂
- 第 4 题 (2) 答案是「a=3」，若有唯一解的条件应为 a≠3，请确认原稿是否写错
```

**最后在对话里也要把「订正记录」和「待你核对」原样说一遍**，别只说「完成了」。

---

## 出错时

- **切不出题**（DONE 0）：`作业.txt` 里没有 `\noindent\textbf{...}` 标记。补上再跑第 4 步，不用重跑第 2 步。
- **npm install 失败**：检查网络；或把已有的 `node_modules` 目录（含 `mathjax`、`puppeteer-core`）拷进 `$SKILL/scripts/`。
- **找不到 Chrome**：装 Chrome 或 Edge；或改 `hw_render.py` 里的 `CHROME_CANDIDATES`。
- **公式渲染成红色报错文字**：多半是用了渲染端不认识的命令。看图定位，改掉重跑第 4 步。

## 只改图不重写

用户改了 `latex/作业.txt` 之后，直接重跑第 4 步即可 —— 不需要重新读图转写。
